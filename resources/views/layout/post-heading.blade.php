<div class="flex items-center gap-2">
    <img src="{{ $avatar }}" alt="{{ $user->name }}" class="fi-avatar fi-circular fi-size-md fi-user-avatar">

    <h2 class="text-xl font-bold">
        {{ $user->name }} | {{ $title }}
    </h2>
</div>